import React, { Fragment } from 'react';

import { Routing } from 'routing/Routing';
import { GlobalStyle } from 'ui';

const App = () => (
	<Fragment>
		<Routing />
		<GlobalStyle />
	</Fragment>
);

export default App;
